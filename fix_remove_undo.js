const fs = require("fs");const path = "D:\\Users\\Downloads\\develop\\slidecleaner\\app\\src\\main\\java\\com\\gallery\\cleaner\\ui\\screen\\media\\MediaSwipeScreen.kt";let c = fs.readFileSync(path, "utf-8");const NL = "\r\n";c = c.replace("            onUndo: (() -> Unit)? = null," + NL + "    onRedo: (() -> Unit)? = null" + NL + ") {", "    ) {");c = c.replace("            onUndo = { viewModel.undo() }," + NL + "            onRedo = { viewModel.redo() }" + NL + "        )", "        )");let undoRedoRow = "            Row(horizontalArrangement = Arrangement.spacedBy(AppPadding.XS)) {" + NL + "                SwipeStatusChip(" + NL + "                    label = if (canUndo) " + String.fromCharCode(34) + "UNDO ON" + String.fromCharCode(34) + " else " + String.fromCharCode(34) + "UNDO" + String.fromCharCode(34) + "," + NL + "                    tint = if (canUndo) AppColors.Primary else AppColors.TextDisabled," + NL + "                    onClick = onUndo" + NL + "                )" + NL + "                SwipeStatusChip(" + NL + "                    label = if (canRedo) " + String.fromCharCode(34) + "REDO ON" + String.fromCharCode(34) + " else " + String.fromCharCode(34) + "REDO" + String.fromCharCode(34) + "," + NL + "                    tint = if (canRedo) AppColors.Accent else AppColors.TextDisabled," + NL + "                    onClick = onRedo" + NL + "                )" + NL + "            }";c = c.replace(undoRedoRow, "");fs.writeFileSync(path, c, "utf-8");console.log("Done");